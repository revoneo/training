{
  "name": "training",
  "version": "0.0.0",
  "description": "training",
  "main": "app.js",
  "scripts": {
    "log": "pm2 log training",
    "start": "pm2 start app.js --name training --watch && npm run log",
    "stop": "pm2 kill",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "koajs"
  ],
  "author": "S-kazuki <contact@revoneo.com>",
  "license": "MIT",
  "dependencies": {
    "koa": "^2.5.1",
    "koa-bodyparser": "^4.2.1",
    "koa-logger": "^3.2.0",
    "koa-router": "^7.4.0",
    "mongoose": "^5.2.3",
    "pm2": "^3.0.0"
  }
}
